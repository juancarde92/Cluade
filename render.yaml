services:
  - type: web
    name: calificador-hojas-de-vida
    runtime: python
    plan: free
    rootDir: app
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120
